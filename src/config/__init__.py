"""Configuration package for the monitoring & optimization layer (Part 2).

Holds pricing tables (chat + embedding, cross-provider) and tunable thresholds
for the optimizer. Everything here is overridable via environment variables so
deployments never need code changes to retune pricing or limits.
"""
